<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newBurger="{{devoured}}">
		{{#if devoured}}CONSUME!{{else}}CONSUME ME!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
